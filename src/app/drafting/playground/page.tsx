import DraftingStudio from '@/components/drafting/DraftingStudio';

export default function DraftingPlaygroundPage() {
    return (
        <div style={{ height: '100vh', display: 'flex', flexDirection: 'column' }}>
            <DraftingStudio />
        </div>
    );
}
